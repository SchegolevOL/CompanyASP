@model CompanyASP.Models.Department


@{
    ViewData["Title"] = "AddDepartment";
}

<h1>AddDepartment</h1>

<h4>Department</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddDepartment">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ParentDepartmentID" class="control-label"></label>

                @*<select asp-for="ParentDepartmentID" name="SelectDepartment" id="SelectDepartment">
                    <option value=""></option>
                    @foreach (var item in ViewBag.Departments)
                    {
                        <option value="@item.Id">@item.Id</option>
                    }

                </select>*@

                <select asp-for="ParentDepartmentID" asp-items="ViewBag.Department">
                <option selected="selected" disabled="disabled"></option>
                </select>

                @*<select asp-for="ParentDepartmentID" asp-items="ViewBag.Departments"></select>*@


                @*<input asp-for="ParentDepartmentID" class="form-control" value="" />
                <span asp-validation-for="ParentDepartmentID" class="text-danger"></span>*@
            </div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Code" class="control-label"></label>
                <input asp-for="Code" class="form-control" />
                <span asp-validation-for="Code" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
